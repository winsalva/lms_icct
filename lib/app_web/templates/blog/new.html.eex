<%= render(AppWeb.SharedView, "back-navigation.html", conn: @conn, title: "New blog") %>

<div class="sub-container">

  <%= form_for @blog, Routes.blog_path(@conn, :create), fn f -> %>
<%= if @blog.action do %>
<div class="alert alert-danger">
  <p>Oops, something went wrong. Please check the errors below.</p>
</div>
<% end %>

<label>Title</label>
<%= text_input f, :title, placeholder: "Blog title.." %>
<%= error_tag f, :title %>

<label>Thumbnail</label>
<%= text_input f, :thumbnail %>
<%= error_tag f, :thumbnail %>

<label>Content</label>
<%= textarea f, :body %>
<%= error_tag f, :body %>

<%= submit "Save" %>
<% end %>
</div>
